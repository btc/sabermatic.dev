# After editing, run:
#   terraform init -backend-config="bucket=<project_id>-tfstate"
#   terraform plan
#   terraform apply

project_id  = "drill-prod"
region      = "us-central1"
environment = "prod"
github_repo = "btc/drill"
